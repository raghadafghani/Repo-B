{
  "parameters": {
    "learning_rate": {
      "type": "float",
      "default": 0.001,
      "min": 0.0001,
      "max": 0.1,
      "description": "Learning rate for model training"
    },
    "batch_size": {
      "type": "integer", 
      "default": 32,
      "min": 1,
      "max": 512,
      "description": "Batch size for training"
    },
    "epochs": {
      "type": "integer",
      "default": 100,
      "min": 1,
      "max": 1000,
      "description": "Number of training epochs"
    },
    "model_type": {
      "type": "string",
      "default": "transformer",
      "options": ["transformer", "lstm", "cnn"],
      "description": "Type of model architecture"
    }
  }
}